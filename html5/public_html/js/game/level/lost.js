
/* == START FILE ========================================================= */

var Lost = function(parent)
{
    var that = {
        
        
    };
    return that;
};

/* == END FILE ========================================================== */
